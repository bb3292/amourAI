# ═══════════════════════════════════════════════════════════════
# RivalIQ Configuration – Tech Stack: Blaxel + Anthropic + White Circle + Lovable
# ═══════════════════════════════════════════════════════════════

# ── Anthropic (Required) ──────────────────────────────────────
# LLM provider for all agent logic (used directly or via Blaxel gateway)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ── Blaxel (Optional) ────────────────────────────────────────
# Agent orchestration platform — routes LLM calls through Blaxel model gateway
# with unified telemetry, fallback, and credential management.
# If not set, falls back to direct Anthropic API calls.
BLAXEL_API_KEY=
BLAXEL_WORKSPACE=
BLAXEL_MODEL_NAME=claude-sonnet

# ── White Circle AI (Optional) ───────────────────────────────
# Quality monitoring and evaluation — hallucination detection,
# relevance scoring, evidence coverage, and guardrails.
# If not set, falls back to built-in LLM-as-judge evaluator.
WHITECIRCLE_API_KEY=
WHITECIRCLE_BASE_URL=https://api.whitecircle.ai/v1

# ── Server Settings ──────────────────────────────────────────
BACKEND_PORT=8000
FRONTEND_PORT=5173
